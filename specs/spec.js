var assert = require( "assert" );
var Hero = require( "../hero" );
var You = require( "../you" );

var chorus = function() {
  describe( "Hero", function() {
    it("can be your hero baby");
    it("can kiss away the pain");
    it("will stand by you forever");
    it("can have breath taken away by you")
  })
};

chorus();